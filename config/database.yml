SQLite version 3.x
  gem install sqlite3

  Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#
default: &default
  adapter: sqlite3
  pool: 5
  timeout: 5000

development:
  <<: *default
  database: db/development.sqlite3

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: db/test.sqlite3

# production:
#   <<: *default
#   database: db/production.sqlite3

production:
#半角スペース２個分の空白を開ける
  <<: *default
  adapter: postgresql
  encoding: unicode
  pool: 5

# #mysqlの設定
# default: &default
#   adapter: mysql2
#   encoding: utf8
#   pool: 5
#   username: root
#   password:
#   host: localhost


# development:
#   <<: *default
#   database: PictureThread_development #　DBの名前。「sample」の部分をアプリの名前に変更すると分かりやい

# test:
#   <<: *default
#   database: PictureThread_test #　DBの名前。「sample」の部分をアプリの名前に変更すると分かりやい

# production:
#   <<: *default
#   database: PictureThread_production
#   username: root
#   password: 